# Prac1_214
Factory Method pattern, Prototype pattern, Template Method pattern, Memento pattern


# Student Number
u25144465

# Questions

# 3.5
The run function is not overridden in the derived classes therefore it does not need to be virtual. The code for run() will choose between the two derived classes. That code should not change, it is not default code. Therefore the function should not be virtual.

# 3.6
